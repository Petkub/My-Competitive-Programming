#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
using vi = vector<int>;
using pll = pair<ll, ll>;
const char nl = '\n';

int main()
{
    int n;
    cin >> n;
    vector<pll> cake;

    while (n--)
    {
        int A;
        cin >> A;
        int count = 1;
        while (A % 2 == 0)
        {
            A /= 2;
            count++;
        }

    }
    
    return (0);
}