#include <bits/stdc++.h>
using namespace std;
using vi = vector<int>;
using vvi = vector<vi>;
#define MAX_COL 10

int main()
{
    cin.tie(0); ios::sync_with_stdio(false);
    int n;
    cin >> n;
    vvi order(3, vi(MAX_COL, 0));
    
    return (0);
}