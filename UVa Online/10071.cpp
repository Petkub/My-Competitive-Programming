#include <bits/stdc++.h>
using namespace std;

int main()
{
    cin.tie(nullptr); ios::sync_with_stdio(false);

    int v, t;
    while (cin >> v && cin >> t)
    {
        int d = 2 * v * t;
        cout << d << '\n';
    }
    return (0);
}