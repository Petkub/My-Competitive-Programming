#include<bits/stdc++.h>
using namespace std;
#define int long long

int32_t main()
{
    cin.tie(0);ios::sync_with_stdio(0);
    int a, b;
    while(cin>>a>>b)
    {
        cout<<abs(a-b)<<'\n';
    }
    return 0;
}