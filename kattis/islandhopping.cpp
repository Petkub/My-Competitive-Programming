#include<bits/stdc++.h>
using namespace std;
#ifdef LOCAL
#define debug(x) cerr << #x << " = "<< x <<"\n";
#else
#define debug(x)
#endif
#define int long long

int32_t main()
{
    cin.tie(0);ios::sync_with_stdio(0);
    int tc; cin>>tc;
    while(tc--)
    {
        
    }
    return 0;
}