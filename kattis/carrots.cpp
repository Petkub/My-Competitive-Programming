#include<bits/stdc++.h>
using namespace std;
#define int long long

int32_t main()
{
    cin.tie(0);ios::sync_with_stdio(0);
    int n, p; cin>>n>>p;
    for(int i=0;i<n;i++)
    {
        string s;
        getline(cin,s);
    }
    cout<<p;
    return 0;
}