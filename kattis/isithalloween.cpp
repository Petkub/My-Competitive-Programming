#include<bits/stdc++.h>
using namespace std;
#define int long long

int32_t main()
{
    cin.tie(0); ios::sync_with_stdio(0);
    string date; getline(cin,date);
    if(date=="OCT 31" or date=="DEC 25") cout<<"yup";
    else cout<<"nope";
    return 0;
}