#include <bits/stdc++.h>
using namespace std;
using str = string;
const char nl = '\n';
int main()
{
    cin.tie(nullptr); ios::sync_with_stdio(false);

    str s;
    while (getline(cin, s))
    {
        cout << s << nl;
    }
    return (0);
}