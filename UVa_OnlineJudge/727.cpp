#include<bits/stdc++.h>
using namespace std;
#define int long long

int32_t main()
{
    cin.tie(0); ios::sync_with_stdio(0);
    int tc; cin>>tc;
    cin.ignore();
    cin.ignore();
    while(tc--)
    {
        string s;
        while(getline(cin,s) and !s.empty())
        {
            char ch=s[0];
            queue<int> q;
            stack<char> stk;
            if(isdigit(ch)) q.push(ch);
            else
            {
                
            }
        }
    }
    return 0;
}